# Chapter 9.4: Pentium Architecture

## 📚 Chapter Overview

This chapter covers the Intel Pentium processor (P5), which introduced superscalar execution, dual pipelines, and branch prediction, representing a major leap in x86 performance.

---

## 🎯 Learning Objectives

After completing this chapter, you will be able to:
- Understand superscalar architecture principles
- Explain dual pipeline execution (U and V pipes)
- Describe branch prediction mechanisms
- Understand the 64-bit data bus
- Compare Pentium improvements over 80486

---

## 1. Pentium Architecture Overview

### 1.1 Basic Specifications

```
PENTIUM PROCESSOR SPECIFICATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────────────────────────────────────────────────────────┐
│                                                                │
│   Year Introduced:     1993 (P5)                               │
│   Transistor Count:    3.1 Million                             │
│   Process Technology:  0.8 µm → 0.35 µm                        │
│   Clock Speed:         60, 66, 75, 90, 100, 120, 133,          │
│                        150, 166, 200 MHz                       │
│                                                                │
│   Data Bus Width:      64 bits (internal 32-bit)               │
│   Address Bus Width:   32 bits                                 │
│   Physical Memory:     4 GB                                    │
│   Virtual Memory:      64 TB                                   │
│                                                                │
│   L1 Cache:           16 KB (8 KB Code + 8 KB Data)            │
│   On-chip FPU:        Yes, enhanced                            │
│   Pipelines:          2 (U-pipe and V-pipe) - Superscalar!     │
│   Pipeline Stages:    5 stages each                            │
│   Branch Prediction:  Yes, BTB                                 │
│   Package:            273-pin PGA, 296-pin SPGA                │
│                                                                │
│   Variants:                                                    │
│   - Pentium (P5):     Original                                 │
│   - Pentium (P54C):   Improved, 0.6 µm                         │
│   - Pentium MMX:      Multimedia extensions                    │
│   - Pentium OverDrive:Upgrade for 486 systems                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘

MAJOR IMPROVEMENTS OVER 80486:
─────────────────────────────
✓ Superscalar - Two instructions per clock
✓ 64-bit external data bus (faster memory)
✓ Separate code and data caches (Harvard-style)
✓ Branch prediction with BTB
✓ Enhanced FPU (8× faster than 486)
✓ Advanced paging (4 MB pages)
✓ System Management Mode (SMM) standard
✓ On-chip APIC for multiprocessing
✓ Performance monitoring counters
```

### 1.2 Block Diagram

```
PENTIUM INTERNAL ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────────────────────────────┐
│                              PENTIUM CPU                                     │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                        Bus Interface Unit                              │ │
│  │   ┌──────────────────┐  ┌─────────────────┐  ┌───────────────────────┐│ │
│  │   │ Address (32-bit) │  │ Data (64-bit)   │  │ Burst/Pipelined Bus  ││ │
│  │   └──────────────────┘  └─────────────────┘  └───────────────────────┘│ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                          ▲                ▲                                  │
│  ┌───────────────────────┴────┐  ┌────────┴───────────────────────┐         │
│  │      CODE CACHE (8 KB)     │  │       DATA CACHE (8 KB)        │         │
│  │   2-way Set Associative    │  │   2-way Set Associative        │         │
│  │   32-byte line, Write-back │  │   32-byte line, Write-back     │         │
│  │   + MESI Protocol          │  │   + MESI Protocol              │         │
│  └─────────────┬──────────────┘  └────────────────┬───────────────┘         │
│                │                                   │                         │
│  ┌─────────────┴──────────────────────────────────┴───────────────┐         │
│  │                      PREFETCH BUFFERS                          │         │
│  └─────────────────────────────────┬──────────────────────────────┘         │
│                                    │                                         │
│  ┌─────────────────────────────────┴──────────────────────────────┐         │
│  │              BRANCH TARGET BUFFER (BTB) - 256 entries          │         │
│  └─────────────────────────────────┬──────────────────────────────┘         │
│                                    │                                         │
│  ┌─────────────────────────────────┴──────────────────────────────┐         │
│  │                    INSTRUCTION DECODE                          │         │
│  │             (Decodes 2 instructions per clock)                 │         │
│  └──────────┬──────────────────────────────────────┬──────────────┘         │
│             │                                      │                         │
│  ┌──────────▼──────────┐            ┌──────────────▼──────────────┐         │
│  │     U-PIPELINE      │            │      V-PIPELINE             │         │
│  │    (Primary)        │            │     (Secondary)             │         │
│  │  ┌────────────────┐ │            │  ┌────────────────┐         │         │
│  │  │ PF - Prefetch  │ │            │  │ PF - Prefetch  │         │         │
│  │  │ D1 - Decode 1  │ │            │  │ D1 - Decode 1  │         │         │
│  │  │ D2 - Decode 2  │ │            │  │ D2 - Decode 2  │         │         │
│  │  │ EX - Execute   │ │            │  │ EX - Execute   │         │         │
│  │  │ WB - Write Back│ │            │  │ WB - Write Back│         │         │
│  │  └────────────────┘ │            │  └────────────────┘         │         │
│  │                     │            │                              │         │
│  │ All Instructions    │            │ Simple Instructions Only    │         │
│  └─────────────────────┘            └──────────────────────────────┘         │
│                │                                    │                        │
│  ┌─────────────┴────────────────────────────────────┴───────────────┐       │
│  │                      FLOATING POINT UNIT                         │       │
│  │   8-stage pipeline  │  Pipelined Adder, Multiplier, Divider      │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │                    Control Unit + APIC                            │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Superscalar Architecture

### 2.1 Dual Pipeline Design

```
DUAL PIPELINE EXECUTION
━━━━━━━━━━━━━━━━━━━━━━━

U-PIPELINE (Primary):
───────────────────
• Executes ALL integer and FP instructions
• Can handle complex instructions
• Full 486-compatible execution

V-PIPELINE (Secondary):
─────────────────────
• Executes ONLY "simple" instructions
• Limited instruction set
• Must pair with U-pipe instruction

PAIRABLE INSTRUCTIONS (V-pipe):
─────────────────────────────
✓ MOV (register/memory)
✓ ADD, SUB, AND, OR, XOR
✓ INC, DEC
✓ PUSH, POP (register)
✓ LEA, NOP
✓ CMP, TEST
✓ Conditional jumps (JCC)

NOT PAIRABLE (U-pipe only):
─────────────────────────
✗ MUL, DIV, IMUL, IDIV
✗ Shifts by CL
✗ String instructions (MOVS, etc.)
✗ Protected mode instructions
✗ FP instructions (separate FP pipeline)

PAIRING RULES:
────────────

┌──────────────────────────────────────────────────────────────────┐
│                    INSTRUCTION PAIRING                           │
│                                                                  │
│  For two consecutive instructions to pair:                       │
│                                                                  │
│  1. Both must be simple (pairable)                               │
│  2. No data dependency (register/flag)                           │
│  3. No AGI stall (Address Generation Interlock)                  │
│  4. First instruction not a branch                               │
│  5. Both fit in same aligned 32-byte block                       │
│                                                                  │
│  Example - WILL PAIR:                                            │
│  ┌────────────────┐ ┌────────────────┐                          │
│  │ MOV EAX, [mem] │ │ ADD EBX, ECX   │  ← No dependency         │
│  │   (U-pipe)     │ │   (V-pipe)     │                          │
│  └────────────────┘ └────────────────┘                          │
│                                                                  │
│  Example - WILL NOT PAIR:                                        │
│  ┌────────────────┐ ┌────────────────┐                          │
│  │ MOV EAX, [mem] │ │ ADD EAX, EBX   │  ← Data dependency!      │
│  │   (U-pipe)     │ │   (stall)      │                          │
│  └────────────────┘ └────────────────┘                          │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 Pipeline Execution

```
SUPERSCALAR EXECUTION IN ACTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CODE SEQUENCE:
─────────────
1:  MOV EAX, [data1]   ; Load EAX
2:  ADD EBX, ECX       ; Add (no dependency - pairs!)
3:  MOV EDX, [data2]   ; Load EDX
4:  SUB ESI, EDI       ; Sub (no dependency - pairs!)
5:  INC EAX            ; Increment (depends on 1)
6:  DEC EBX            ; Decrement (depends on 2)

EXECUTION TIMELINE:
─────────────────

Clock:  │  1  │  2  │  3  │  4  │  5  │  6  │  7  │
━━━━━━━━┿━━━━━┿━━━━━┿━━━━━┿━━━━━┿━━━━━┿━━━━━┿━━━━━┤
U-pipe: │ 1-PF│ 1-D1│ 1-D2│ 1-EX│ 1-WB│     │     │
V-pipe: │ 2-PF│ 2-D1│ 2-D2│ 2-EX│ 2-WB│     │     │
        ├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
U-pipe: │     │ 3-PF│ 3-D1│ 3-D2│ 3-EX│ 3-WB│     │
V-pipe: │     │ 4-PF│ 4-D1│ 4-D2│ 4-EX│ 4-WB│     │
        ├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
U-pipe: │     │     │ 5-PF│ 5-D1│ 5-D2│ 5-EX│ 5-WB│
V-pipe: │     │     │ 6-PF│ 6-D1│ 6-D2│ 6-EX│ 6-WB│

6 instructions in 7 clocks!
(Single pipeline would take 10+ clocks)

THEORETICAL PEAK: 2 instructions per clock (IPC = 2)
PRACTICAL AVERAGE: 1.5-1.8 IPC (due to dependencies, stalls)
```

---

## 3. Branch Prediction

### 3.1 Branch Target Buffer (BTB)

```
BRANCH PREDICTION MECHANISM
━━━━━━━━━━━━━━━━━━━━━━━━━━━

PURPOSE:
───────
Predict branch outcomes to avoid pipeline stalls.
Wrong prediction = 3-4 cycle penalty (pipeline flush)

BRANCH TARGET BUFFER (BTB):
─────────────────────────

┌──────────────────────────────────────────────────────────────────┐
│                    BTB - 256 Entries                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Entry │ Valid │ Branch Address │ Target Address │ History │  │
│  ├───────┼───────┼────────────────┼────────────────┼─────────┤  │
│  │   0   │   1   │   0040100C     │   00401050     │   11    │  │
│  │   1   │   1   │   00401234     │   00401300     │   10    │  │
│  │   2   │   0   │      -         │       -        │    -    │  │
│  │  ...  │  ...  │     ...        │      ...       │   ...   │  │
│  │  255  │   1   │   004015A0     │   00401000     │   11    │  │
│  └───────┴───────┴────────────────┴────────────────┴─────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

HISTORY BITS (2-bit Saturating Counter):
──────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   State Diagram:                                                │
│                                                                 │
│   ┌────────────────────────────────────────────────────────┐   │
│   │                                                        │   │
│   │   00 (Strongly Not Taken)                              │   │
│   │       │                                                │   │
│   │       │ Taken                                          │   │
│   │       ▼                                                │   │
│   │   01 (Weakly Not Taken)                                │   │
│   │       │                                                │   │
│   │       │ Taken                                          │   │
│   │       ▼                                                │   │
│   │   10 (Weakly Taken)      ◄─── PREDICTION THRESHOLD     │   │
│   │       │                                                │   │
│   │       │ Taken                                          │   │
│   │       ▼                                                │   │
│   │   11 (Strongly Taken)                                  │   │
│   │                                                        │   │
│   └────────────────────────────────────────────────────────┘   │
│                                                                 │
│   Prediction: If bit 1 = 1, predict TAKEN                      │
│               If bit 1 = 0, predict NOT TAKEN                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Branch Prediction Operation

```
BRANCH PREDICTION FLOW
━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│   1. FETCH stage: Compare branch address with BTB                   │
│                                                                     │
│   2. IF BTB hit AND predicted TAKEN:                                │
│      → Start fetching from target address                           │
│                                                                     │
│   3. IF BTB miss OR predicted NOT TAKEN:                            │
│      → Continue sequential fetch                                    │
│                                                                     │
│   4. At EXECUTE stage: Actual branch outcome known                  │
│                                                                     │
│   5. IF prediction was CORRECT:                                     │
│      → No penalty, update history                                   │
│                                                                     │
│   6. IF prediction was WRONG:                                       │
│      → Flush pipeline (3-4 cycle penalty)                          │
│      → Start fetch from correct address                             │
│      → Update BTB with correct info                                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

PREDICTION ACCURACY:
──────────────────

Loop (back edge):     ~90% accurate
If-else (forward):    ~75% accurate
Function return:      ~90% (with RSB in later Pentiums)

Misprediction penalty: 3-4 clock cycles

OPTIMIZATION TIP:
───────────────

; Arrange code so likely path is fall-through
    CMP EAX, 0
    JZ  rare_case      ; Jump is rare (predict not taken)
    ; Common path here (no penalty)
    ...
rare_case:
    ; Rare path here
    ...
```

---

## 4. Cache System

### 4.1 Split L1 Cache

```
PENTIUM CACHE ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━

SPLIT CACHE DESIGN (Harvard-style):
─────────────────────────────────

┌───────────────────────────────────────────────────────────────────┐
│                                                                   │
│  ┌─────────────────────────┐    ┌─────────────────────────┐      │
│  │    CODE CACHE (8 KB)    │    │    DATA CACHE (8 KB)    │      │
│  │                         │    │                         │      │
│  │  Instruction-only       │    │  Data read/write        │      │
│  │  2-way set associative  │    │  2-way set associative  │      │
│  │  32-byte line           │    │  32-byte line           │      │
│  │  Read-only              │    │  Write-back (MESI)      │      │
│  │  128 sets               │    │  128 sets               │      │
│  │                         │    │                         │      │
│  └────────────┬────────────┘    └───────────┬─────────────┘      │
│               │                              │                    │
│               │ Instruction                  │ Data               │
│               │ Fetch                        │ Load/Store         │
│               ▼                              ▼                    │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │                 CPU Execution Units                          ││
│  └──────────────────────────────────────────────────────────────┘│
│                                                                   │
└───────────────────────────────────────────────────────────────────┘

ADVANTAGES OF SPLIT CACHE:
────────────────────────
1. Simultaneous instruction fetch + data access
2. No cache conflicts between code and data
3. Optimized for different access patterns
   - Code: Sequential, read-only
   - Data: Random, read/write

CACHE LINE STRUCTURE:
───────────────────

┌──────┬───────────────────┬────────────────────────────────────┐
│Valid │      Tag          │              Data (32 bytes)       │
│(1bit)│    (20 bits)      │                                    │
└──────┴───────────────────┴────────────────────────────────────┘
```

### 4.2 MESI Cache Coherency

```
MESI PROTOCOL (For Multiprocessor Support)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STATES:
──────

┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  M - MODIFIED                                                    │
│      • Data modified, only in this cache                         │
│      • Must write back before another processor reads            │
│      • Memory copy is stale                                      │
│                                                                  │
│  E - EXCLUSIVE                                                   │
│      • Data unmodified, only in this cache                       │
│      • Can transition to M without bus transaction               │
│      • Memory copy is valid                                      │
│                                                                  │
│  S - SHARED                                                      │
│      • Data unmodified, may be in other caches                   │
│      • Must broadcast on write (→ invalidate others)             │
│      • Memory copy is valid                                      │
│                                                                  │
│  I - INVALID                                                     │
│      • Cache line not valid                                      │
│      • Must fetch from memory or other cache                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

STATE TRANSITIONS:
────────────────

        ┌─────────────────────────────────────────┐
        │                                         │
        │              ┌───────────┐              │
        │        ┌────►│     E     │◄────┐       │
        │        │     │ Exclusive │     │       │
        │    Read│     └─────┬─────┘     │Read   │
        │    Miss│           │Write      │Miss   │
        │        │           ▼           │(other)│
   ┌────┴────┐   │     ┌───────────┐     │  ┌────┴────┐
   │    I    │───┘     │     M     │     └──│    I    │
   │ Invalid │         │ Modified  │        │ Invalid │
   └────┬────┘         └───────────┘        └─────────┘
        │                    │
        │ Read Miss          │ Snoop (other reads)
        │ (shared)           ▼
        │              ┌───────────┐
        └─────────────►│     S     │
                       │  Shared   │
                       └───────────┘
```

---

## 5. 64-Bit Data Bus

```
64-BIT EXTERNAL DATA BUS
━━━━━━━━━━━━━━━━━━━━━━━━

ARCHITECTURE:
────────────

Internal: 32-bit registers and ALU (still 32-bit CPU!)
External: 64-bit data bus

┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│    ┌──────────────────────────────────────────────────────────┐   │
│    │                    PENTIUM CPU                            │   │
│    │                                                          │   │
│    │   32-bit Internal Data Paths                              │   │
│    │                                                          │   │
│    │   ┌─────────────────────────────────────────────────┐   │   │
│    │   │           64-bit Data Buffer                     │   │   │
│    │   │   Converts between 32-bit and 64-bit            │   │   │
│    │   └───────────────────────┬─────────────────────────┘   │   │
│    └───────────────────────────┼─────────────────────────────┘   │
│                                │                                  │
│                                │ 64 bits                          │
│                                ▼                                  │
│    ┌───────────────────────────────────────────────────────────┐ │
│    │                    MEMORY SUBSYSTEM                        │ │
│    │                                                           │ │
│    │   64-bit wide DRAM (2 × 32-bit modules)                   │ │
│    │                                                           │ │
│    └───────────────────────────────────────────────────────────┘ │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

BENEFITS:
────────
• Cache line fill (32 bytes) in 4 transfers instead of 8
• 2× memory bandwidth at same clock speed
• Better burst mode efficiency (2-1-1-1 for 32 bytes)

TIMING COMPARISON:
────────────────

                    32-bit Bus     64-bit Bus
Cache line (32B):   8 transfers    4 transfers
Burst fill:         ~10 clocks     ~6 clocks
Bandwidth @ 66MHz:  ~264 MB/s      ~528 MB/s
```

---

## 6. Enhanced Features

### 6.1 Floating Point Unit

```
PENTIUM FPU IMPROVEMENTS
━━━━━━━━━━━━━━━━━━━━━━━━

8-STAGE FPU PIPELINE:
───────────────────

┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│   PF   │→│  D1    │→│  D2    │→│  E1    │→│  E2    │→│  WF    │→│  ER    │→│  WB    │
│Prefetch│ │Decode1 │ │Decode2 │ │Execute1│ │Execute2│ │FP Wait │ │Error   │ │Write   │
└────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘

FPU EXECUTION UNITS:
──────────────────

┌──────────────────────────────────────────────────────────────────┐
│                      FPU Pipeline                                │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ • Pipelined Adder (3-cycle latency)                        │ │
│  │ • Pipelined Multiplier (3-cycle latency)                   │ │
│  │ • Hardware Divider                                         │ │
│  │ • Hardware Square Root                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘

PERFORMANCE COMPARISON:
─────────────────────

Instruction  │ 486DX   │ Pentium  │ Speedup
─────────────┼─────────┼──────────┼─────────
FADD         │ 8-20    │  3       │  3-7×
FMUL         │ 16      │  3       │  5×
FDIV         │ 73      │  39      │  2×
FSQRT        │ 83-87   │  70      │  1.2×
─────────────┴─────────┴──────────┴─────────

Note: Pentium FPU can overlap multiply and add!
```

### 6.2 New Features

```
ADDITIONAL PENTIUM FEATURES
━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 4 MB PAGES (Page Size Extension):
───────────────────────────────────
• CR4.PSE bit enables
• For mapping large regions (kernel, buffers)
• Reduces TLB misses

┌────────────────────────────────────────────────────────────────┐
│  Linear Address with 4 MB Pages:                               │
│                                                                │
│  Bit 31          22 21                                   0     │
│  ┌─────────────────┬─────────────────────────────────────┐    │
│  │   Directory     │              Offset (22 bits)        │    │
│  │   (10 bits)     │              4 MB page               │    │
│  └─────────────────┴─────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────┘

2. PERFORMANCE MONITORING:
────────────────────────
• Model-Specific Registers (MSRs)
• Two 40-bit counters
• Can count: cache hits/misses, branches, etc.

RDMSR  ; Read MSR (ECX = MSR#, EDX:EAX = value)
WRMSR  ; Write MSR
RDTSC  ; Read Time Stamp Counter (cycles since reset)
RDPMC  ; Read Performance Monitoring Counter (Pentium Pro+)

3. MACHINE CHECK EXCEPTION:
─────────────────────────
• Exception #18
• For hardware error reporting
• ECC memory errors, bus errors

4. APIC (Advanced PIC):
─────────────────────
• On-chip for multiprocessor support
• Inter-processor interrupts
• Local timer interrupt

5. DEBUG EXTENSIONS:
──────────────────
• Breakpoint on I/O access
• Enhanced debug registers
```

---

## 7. Pentium MMX

```
PENTIUM MMX (1997)
━━━━━━━━━━━━━━━━━━

SPECIFICATIONS:
─────────────
• 4.5 million transistors
• Same superscalar architecture
• 166, 200, 233 MHz
• 32 KB L1 cache (16 KB + 16 KB)
• 57 new MMX instructions

MMX REGISTERS (Aliased on FPU):
─────────────────────────────

        63                                  0
       ┌─────────────────────────────────────┐
  MM0  │                                     │ = FP ST(0)
       ├─────────────────────────────────────┤
  MM1  │                                     │ = FP ST(1)
       ├─────────────────────────────────────┤
  ...  │                                     │
       ├─────────────────────────────────────┤
  MM7  │                                     │ = FP ST(7)
       └─────────────────────────────────────┘
                64-bit MMX registers

DATA TYPES:
──────────

┌─────────────────────────────────────────────────────────────────┐
│  8 × 8-bit bytes    (packed bytes)                              │
│  ┌────┬────┬────┬────┬────┬────┬────┬────┐                     │
│  │ B7 │ B6 │ B5 │ B4 │ B3 │ B2 │ B1 │ B0 │                     │
│  └────┴────┴────┴────┴────┴────┴────┴────┘                     │
│                                                                 │
│  4 × 16-bit words   (packed words)                              │
│  ┌──────────┬──────────┬──────────┬──────────┐                 │
│  │    W3    │    W2    │    W1    │    W0    │                 │
│  └──────────┴──────────┴──────────┴──────────┘                 │
│                                                                 │
│  2 × 32-bit doublewords (packed dwords)                         │
│  ┌──────────────────────┬──────────────────────┐               │
│  │         D1           │         D0           │               │
│  └──────────────────────┴──────────────────────┘               │
│                                                                 │
│  1 × 64-bit quadword                                            │
│  ┌─────────────────────────────────────────────┐               │
│  │                    Q0                        │               │
│  └─────────────────────────────────────────────┘               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

MMX INSTRUCTION CATEGORIES:
─────────────────────────

• Arithmetic: PADDB, PADDW, PADDD, PSUBB, PMULLW, PMADDWD
• Comparison: PCMPEQB, PCMPGTW
• Logical: PAND, POR, PXOR
• Shift: PSLLW, PSRLW, PSRAW
• Pack/Unpack: PACKSSWB, PUNPCKLBW
• Data Transfer: MOVD, MOVQ

EXAMPLE - Image Brightening:
───────────────────────────

; Add 10 to 8 pixels simultaneously
MOVQ MM0, [image_ptr]     ; Load 8 pixels
MOVQ MM1, brightness_val  ; 10,10,10,10,10,10,10,10
PADDUSB MM0, MM1          ; Add with unsigned saturation
MOVQ [image_ptr], MM0     ; Store 8 pixels

; "US" = Unsigned Saturation (clamps to 255, doesn't wrap)
```

---

## 📋 Summary Table

| Feature | 80486 | Pentium |
|---------|-------|---------|
| Transistors | 1.2M | 3.1M |
| Clock Speed | 25-100 MHz | 60-200 MHz |
| Pipelines | 1 | 2 (Superscalar) |
| IPC (Peak) | 1 | 2 |
| L1 Cache | 8 KB Unified | 16 KB Split |
| Data Bus | 32-bit | 64-bit |
| Branch Predict | No | Yes (BTB) |
| Cache Protocol | - | MESI |
| MMX | No | Optional (MMX) |

---

## ❓ Quick Revision Questions

1. **What makes Pentium "superscalar"?**
   <details>
   <summary>Show Answer</summary>
   Pentium has two execution pipelines (U and V) that can execute two instructions simultaneously. When instructions can be paired (no dependencies, both simple), it achieves up to 2 instructions per clock cycle, doubling throughput vs single-pipeline 486.
   </details>

2. **Why did Pentium use a split L1 cache?**
   <details>
   <summary>Show Answer</summary>
   Split cache allows simultaneous instruction fetch and data access (Harvard-style). Code cache is read-only and optimized for sequential access, data cache handles read/write. Eliminates conflicts between code and data access.
   </details>

3. **How does the BTB improve performance?**
   <details>
   <summary>Show Answer</summary>
   Branch Target Buffer predicts branch outcomes and targets before execution. If prediction is correct, pipeline continues without stall. Wrong prediction causes 3-4 cycle flush penalty. 2-bit history provides ~80-90% accuracy for loops.
   </details>

4. **What is MESI protocol used for?**
   <details>
   <summary>Show Answer</summary>
   MESI (Modified, Exclusive, Shared, Invalid) maintains cache coherency in multiprocessor systems. Each cache line has a state. When one CPU writes, it invalidates copies in other caches. Ensures all CPUs see consistent memory.
   </details>

5. **What are MMX registers and why are they aliased?**
   <details>
   <summary>Show Answer</summary>
   MMX registers (MM0-MM7) are 64-bit for SIMD operations. They're aliased on x87 FPU registers to avoid adding new architectural state (OS compatibility). This means you can't mix MMX and FP code without EMMS instruction to clear.
   </details>

6. **Why does Pentium have a 64-bit data bus but 32-bit registers?**
   <details>
   <summary>Show Answer</summary>
   64-bit external bus doubles memory bandwidth for cache fills (32-byte line in 4 transfers vs 8). Internal 32-bit maintains x86 compatibility. The bus interface unit converts between them. This doesn't change the 32-bit ISA.
   </details>

---

## 🧭 Navigation

| Previous | Up | Next |
|----------|-----|------|
| [9.3 80486 Processor](03-80486-processor.md) | [Unit 9 Index](README.md) | [9.5 ARM Architecture](05-arm-architecture.md) |

---

*[← Previous: 80486 Processor](03-80486-processor.md) | [Next: ARM Architecture →](05-arm-architecture.md)*
